interface:
  display_name: "Devibify"
  short_description: "Guide against vibe-coded UI tells"
  default_prompt: "Use $devibify to review and refine this UI so it avoids vibe-coded tells and feels intentional, consistent, and production-ready."
