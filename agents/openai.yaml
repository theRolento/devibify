interface:
  display_name: "Devibify"
  short_description: "Build and audit product-specific web interfaces"
  default_prompt: "Use $devibify to implement or audit this web interface within the requested scope, with product-specific decisions, complete reachable states, and evidence-based verification."

policy:
  allow_implicit_invocation: true
